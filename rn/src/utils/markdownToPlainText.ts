// Moved to the shared package (packages/core). Kept as a re-export so existing
// `@/utils/markdownToPlainText` imports across rn/ keep working unchanged.
export * from '@core/utils/markdownToPlainText';
