// Shared with rn/ via packages/core — see @core/utils/documentDisplay.
export { getDocDisplayName, documentSourceKind } from '@core/utils/documentDisplay';
export type { DocumentSourceKind } from '@core/utils/documentDisplay';
