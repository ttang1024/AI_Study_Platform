/**
 * The shell every top-level panel on the public share page wears — header, media, each tab's
 * panel and the CTA — so the column reads as one stack rather than a pile of loose boxes.
 * Nested boxes (glossary terms, quiz options) stay on the smaller `rounded-2xl` radius.
 */
export const SHARE_CARD =
  'rounded-3xl border border-[var(--border-color)] bg-[var(--bg-sidebar)] '
  + 'shadow-[0_1px_2px_rgba(0,0,0,0.04),0_8px_24px_-16px_rgba(0,0,0,0.18)]';
