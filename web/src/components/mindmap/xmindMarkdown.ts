// Moved to the shared package (packages/core) — the XMindMark/legacy-JSON →
// markmap-markdown conversion was duplicated in rn/. Re-exported so existing
// imports keep working unchanged.
export * from '@core/utils/xmindMarkdown';
