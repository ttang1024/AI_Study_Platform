{
  "name": "@study/core",
  "version": "0.0.0",
  "private": true,
  "description": "Platform-agnostic domain logic shared by web/ and rn/ (services over an injected HttpClient, pure utils, domain types). No runtime dependencies — consumers inject the HTTP client.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "~5.8.2",
    "vitest": "^2.0.0"
  }
}
